function transferRoutes(list){
    if(list && list.length){
        let routes=[]
        routes.push({
            id:'-1',
            icon:'dashboard',
            route:'/dashboard',
            zh:{
                name:'工作台',
            }
        })
    }
}